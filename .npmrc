# Peer-dependency ranges in the Expo/jest toolchain conflict (e.g. jest-expo pulls a
# jest-watch-typeahead that wants jest <=29), and package-lock.json is resolved with this
# setting. EAS Build runs a plain `npm ci`, so it must be the project default.
legacy-peer-deps=true
