// The same policy as Settings → Privacy Policy, reachable from the paywall before someone has
// subscribed (Settings lives inside the subscription-gated tabs).
export { default } from './(tabs)/settings/privacy-policy';
